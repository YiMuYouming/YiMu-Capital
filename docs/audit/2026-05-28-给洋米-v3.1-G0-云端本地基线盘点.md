# 任务 ID：YM-V31-G0

## YM-V31-G0：V3.1 云端/本地只读基线盘点与分叉收口方案

**负责人：** 洋米  
**主控/验收：** 欧米  
**项目：** `/Users/yimu/Documents/YM_Capital/live-dashboard`  
**云端：** `agentuser@43.132.146.234:/home/agentuser/YiMu-Capital`  
**总计划：** `docs/plans/2026-05-28-v3.1-stability-upgrade-plan.md`

---

## 目标

不修改生产环境、不修数据、不重启服务，完成 V3.1 G0 只读盘点：

1. 确认本地和云端当前运行状态。
2. 确认云端 git 分叉原因和代码差异。
3. 确认云端真实运行数据有哪些变动。
4. 确认健康 API、账户状态和 PnL 摘要的关键问题。
5. 给出 G1 云端 git 收口建议，但本任务不执行收口。

---

## 禁止范围

- 禁止 `git reset --hard`。
- 禁止 `git clean`。
- 禁止 `git pull`、`git merge`、`git rebase`。
- 禁止修改、覆盖或删除 `data/*`。
- 禁止重启 `yimu-live-dashboard.service`。
- 禁止对真实 8088 发任何 `POST`、纠错、日结或压力测试。
- 禁止补录 `day_start_price`；本任务只列缺口。

---

## 执行清单

### 1. 本地只读记录

```bash
cd /Users/yimu/Documents/YM_Capital/live-dashboard
git status --short
git rev-parse --short HEAD
lsof -nP -iTCP:8088 -sTCP:LISTEN || true
lsof -nP -iTCP:18088 -sTCP:LISTEN || true
curl -s --max-time 5 http://127.0.0.1:8088/api/health | python3 -m json.tool | head -120
```

记录：

- 本地 HEAD。
- 8088 是否为 SSH tunnel。
- 18088 是否为本地预览代理。
- 本地工作区有哪些运行数据变动。

### 2. 云端服务和 git 分叉

```bash
ssh -o ConnectTimeout=30 -o ServerAliveInterval=15 agentuser@43.132.146.234 'set -e
cd /home/agentuser/YiMu-Capital
systemctl is-active yimu-live-dashboard.service || true
git status --short
git rev-parse --short HEAD
git rev-parse --short origin/main
git rev-list --left-right --count HEAD...origin/main
git log --oneline --decorate --graph --max-count=12 --all'
```

记录：

- 服务状态。
- 云端 HEAD。
- origin/main。
- ahead/behind 数量。
- 云端有哪些 tracked code 文件变动。
- 云端有哪些真实 data/runtime 文件变动。

### 3. 云端本地 commit 与主线对比

```bash
ssh agentuser@43.132.146.234 'set -e
cd /home/agentuser/YiMu-Capital
git show --stat --oneline --no-renames HEAD --
git diff --stat origin/main...HEAD --
git diff --stat HEAD...origin/main --'
```

如果云端有 `widgets/market-overview.js` 工作区变动，继续只读查看：

```bash
ssh agentuser@43.132.146.234 'cd /home/agentuser/YiMu-Capital && git diff -- widgets/market-overview.js'
```

判断：

- 云端本地 commit 是否已经被 GitHub 后续提交等价覆盖。
- 云端工作区代码差异是否应该丢弃、保留或回补到本地。
- G1 是否可以安全把云端代码收口到 `origin/main`。

### 4. 云端 API 关键状态

```bash
ssh agentuser@43.132.146.234 'set -e
curl -s --max-time 5 http://127.0.0.1:8088/api/health | python3 -m json.tool
curl -s --max-time 5 http://127.0.0.1:8088/api/account/state | python3 -m json.tool | head -180
curl -s --max-time 5 http://127.0.0.1:8088/api/pnl/summary | python3 -m json.tool | head -180
curl -s --max-time 5 http://127.0.0.1:8088/api/live/quotes | python3 -m json.tool | head -120'
```

记录：

- `/api/health.status`
- `critical_ok`
- `trade_entry_allowed`
- `degraded_reasons`
- `llm_config.status`
- `quotes.status`
- `account.valuation_complete`
- `account.quote_status`
- 当前持仓中 `_day_start_price=null` 的代码
- 今日清仓中 `realized_today_pnl=null` 的代码
- PnL `today_snapshots` 数量

### 5. SQLite 只读完整性

```bash
ssh agentuser@43.132.146.234 'cd /home/agentuser/YiMu-Capital && .venv/bin/python - <<'"'"'PY'"'"'
import sqlite3
con = sqlite3.connect("data/pnl.db")
try:
    print("integrity_check:", con.execute("PRAGMA integrity_check").fetchone()[0])
    print("trade_records:", con.execute("SELECT count(*) FROM trade_records").fetchone()[0])
    print("intraday_snapshots:", con.execute("SELECT count(*) FROM intraday_snapshots").fetchone()[0])
    print("account_baselines:", con.execute("SELECT count(*) FROM account_baselines").fetchone()[0])
finally:
    con.close()
PY'
```

记录：

- `integrity_check` 是否为 `ok`。
- 三张关键表计数。

### 6. 输出 G1 建议

在交付报告里明确写：

- G1 是否建议收口云端 git。
- 如果建议收口，建议步骤是什么。
- 哪些文件必须保护。
- 哪些云端工作区差异可以丢弃。
- 哪些差异需要先回补到本地或 GitHub。
- 收口后如何验收。

---

## 交付格式

请提交到 Agent Board，内容按下面格式：

```text
状态：DONE / BLOCKED
任务：YM-V31-G0

本地状态：
- HEAD:
- 8088:
- 18088:
- dirty files summary:

云端状态：
- service:
- HEAD:
- origin/main:
- ahead/behind:
- dirty code files:
- dirty runtime/data files:

API 状态：
- health.status:
- critical_ok:
- trade_entry_allowed:
- degraded_reasons:
- llm_config:
- quotes:
- account valuation:
- pnl snapshots:

账户基准缺口：
- 当前持仓 day_start_price 缺失:
- 今日清仓 realized_today_pnl 缺失:

SQLite：
- integrity_check:
- trade_records:
- intraday_snapshots:
- account_baselines:

G1 收口建议：
- 是否建议收口:
- 推荐步骤:
- 必须保护:
- 风险:
- 验收命令:

是否触碰真实数据：
- 否

是否重启服务：
- 否
```

---

## 欧米验收标准

- 报告完整覆盖本任务所有只读检查项。
- 没有执行禁止范围内操作。
- 能清楚回答“云端下一步怎么安全回到 GitHub 主线”。
- 能清楚列出“今天哪些收益分项不可信，原因是什么”。
