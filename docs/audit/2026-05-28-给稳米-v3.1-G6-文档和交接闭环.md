# 任务 ID：WM-V31-G6

## WM-V31-G6：V3.1 文档和交接闭环

**负责人：** 稳米  
**主控/验收：** 欧米  
**项目：** `/Users/yimu/Documents/YM_Capital/live-dashboard`  
**总计划：** `docs/plans/2026-05-28-v3.1-stability-upgrade-plan.md`  
**派发时机：** 等 YM-V31-G5 验收通过后再派发。

---

## 目标

让任意一个米接手弈沐资本看板时，只读一份入口文档就能知道：

- 盘中主人应该打开哪个地址。
- 8088、18088、18089 分别是什么。
- 开盘前怎么生成基线并同步上云。
- 盘中成交应该在哪里录入，哪里不能录入。
- 收盘后怎么把云端数据拉回本地。
- 出问题时先查什么、不能做什么。

本任务只做文档和交接基线，不改业务代码，不改真实数据。

---

## 当前背景

V3.1 的生产形态已经确定：

- 云端 hermes 是生产服务，主人本地 `localhost:8088` 通过 SSH tunnel 访问云端。
- 本地 `localhost:18088` 是只读预览，加载本地代码，读取云端数据，写请求必须拦截。
- 可选 `localhost:18089` 是本地完整服务，只用于诊断，不作为默认生产账本。
- 数据文件不走 git；云端 `data/pnl.db` 是盘中主账本。
- 开盘/收盘脚本已经在 G2 增加：`scripts/ops/open_day.py`、`scripts/ops/close_day.py`。
- G3-G5 会补齐健康语义、生产/预览提示、W04/W22 降级展示；G6 需要把最终口径写进文档。

---

## 禁止范围

- 禁止修改 `data/*`。
- 禁止运行会写云端的命令。
- 禁止对真实 8088 发 `POST`。
- 禁止重启云端服务。
- 禁止修改业务代码、前端组件、测试。
- 禁止把运行数据、快照、SQLite 数据库写入 git。

---

## 文件范围

允许修改：

- `README.md`
- `AGENTS.md`
- `docs/ops/2026-05-28-cloud-data-sync-runbook.md`
- 新增 `docs/audit/2026-05-28-v3.1-completion-baseline.md`

禁止修改：

- `scripts/*`
- `widgets/*`
- `index.html`
- `store.js`
- `css/*`
- `tests/*`
- `data/*`

---

## 行为要求

### 1. README 更新为 V3.1 入口文档

README 至少要讲清楚：

- 当前版本是 V3.1 稳定性收口，而不是旧的 v3.0 单机启动口径。
- `http://localhost:8088`：云端生产，通过 SSH tunnel 到 hermes。
- `http://localhost:18088`：本地预览，只读，用于改组件看效果。
- `http://localhost:18089`：本地完整服务，可选诊断，不默认录真实交易。
- 开盘前用 `python3 scripts/ops/open_day.py --dry-run` / `--apply`。
- 收盘后用 `python3 scripts/ops/close_day.py --dry-run` / `--apply`。
- 代码走 git，数据不走 git。
- 真实成交只在生产 8088 录入。

### 2. AGENTS 更新为协作和生产边界

AGENTS 至少要补充：

- V3.1 的云端生产/本地预览边界。
- 云端 PyTDX 不是生产必需依赖，云端 fallback 为主。
- 代码任务优先交洋米实现、欧米审查；文档/流程任务可交稳米。
- agent-board 是跨 agent 派单和验收入口。
- 遇到 8088/18088 混淆、交易录入、数据同步问题时先看 runbook。

### 3. Runbook 收口

`docs/ops/2026-05-28-cloud-data-sync-runbook.md` 需要和最终代码口径一致：

- 明确 8088/18088/18089 三入口。
- 明确开盘、盘中、收盘流程。
- 明确 `open_day.py` 和 `close_day.py` 是推荐入口。
- 明确 18088 只读，不录真实交易。
- 明确云端数据源降级口径，PyTDX 不作为云端唯一生产依赖。
- 明确数据回流不走 git。

### 4. 新增 V3.1 完成基线

新增：

```text
docs/audit/2026-05-28-v3.1-completion-baseline.md
```

内容至少包括：

- G0-G6 阶段状态表。
- 当前生产拓扑。
- 当前数据源口径。
- 当前开盘/盘中/收盘 SOP。
- 当前健康语义：正常、降级、阻断、无响应。
- 当前已知降级和剩余风险。
- 故障排查顺序。
- 禁止操作清单。
- 下一版可以再优化但 V3.1 不做的事项。

---

## 执行清单

### Step 1：只读确认当前文档和代码口径

运行：

```bash
rg -n "8088|18088|18089|open_day|close_day|YIMU_DISABLE_PYTDX|云端生产|本地预览|只读" README.md AGENTS.md docs/ops/2026-05-28-cloud-data-sync-runbook.md
rg -n "runtimeMode|readonly_dev_preview|account_basis|degraded_reasons|trade_entry_allowed|critical_ok" index.html widgets/positions.js scripts/bridge.py
git status --short
git diff -- data/
```

要求：

- 只读确认，不改数据。
- 如果 G3-G5 还没有验收通过，不要开始 G6；等欧米派发。

### Step 2：更新 README / AGENTS / runbook

要求：

- 文档简洁，入口清楚。
- 不复制大段过期历史。
- 不写“本地 8088 就是本地服务”这种旧口径。
- 不把 PyTDX 写成云端生产强依赖。

### Step 3：新增完成基线

新增完成基线时，要使用当前真实状态：

- 如果 G3-G5 有未完成项，不能写成完成。
- 如果有已知降级，必须写清楚“影响范围”和“是否阻断交易录入”。
- 如果测试或运行检查失败，必须写入剩余风险，不要粉饰。

### Step 4：验证

必须运行：

```bash
rg -n "8088|18088|18089|open_day|close_day|YIMU_DISABLE_PYTDX|云端生产|本地预览|只读" README.md AGENTS.md docs/ops/2026-05-28-cloud-data-sync-runbook.md docs/audit/2026-05-28-v3.1-completion-baseline.md
rg -n "代码走 git|数据不走 git|真实成交|云端生产|本地预览" README.md AGENTS.md docs/ops/2026-05-28-cloud-data-sync-runbook.md docs/audit/2026-05-28-v3.1-completion-baseline.md
git diff -- data/
```

可选运行：

```bash
python3 -m compileall -q scripts tests
```

---

## 通过条件

- README 能作为最短入口文档。
- AGENTS 明确团队协作边界和生产/预览边界。
- runbook 与脚本、端口、数据同步口径一致。
- completion baseline 记录 V3.1 当前真实能力、已知降级、排查顺序。
- `git diff -- data/` 无新增测试或文档污染。
- 没有修改业务代码和真实数据。

---

## 交付格式

提交到 Agent Board：

```text
状态：DONE / BLOCKED
任务：WM-V31-G6

实际修改文件：
- 

每项需求对应实现：
- README 入口:
- AGENTS 协作边界:
- runbook:
- completion baseline:

验证结果：
- rg 入口口径:
- rg 代码/数据边界:
- git diff -- data/:

是否触碰真实数据：
- 否

是否对真实 8088 写操作：
- 否

尚存风险：
- 
```

