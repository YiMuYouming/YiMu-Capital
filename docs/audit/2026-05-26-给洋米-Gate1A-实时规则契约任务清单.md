# Gate 1A 实时规则契约 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增唯一的后端实时规则引擎 `rule_state`，让行情接口和 LLM 快照输出相同的可审计硬规则结果，为后续组件和 AI 校验接线冻结契约。

**Architecture:** `scripts/rule_engine.py` 只做纯函数：接收已标准化的账户、行情情绪、风格和新鲜度输入，输出 `rule_state`；不得读取文件、数据库或全局 CACHE。`scripts/bridge.py` 仅负责把现有 CACHE、dashboard baseline 和账户 SSOT 适配为规则输入，并把同一份结果附加到实时 API 与 LLM snapshot；本包不重写前端组件，也不改 LLM 输出协议。

**Tech Stack:** Python 3 `unittest`、现有纯 HTML/JS dashboard、现有 `BridgeHandler` mock 测试模式。

---

## 一、边界和硬约束

### 1. 本包只做什么

- `YM-G1A-1`：新建实时规则纯函数及阈值常量。
- `YM-G1A-2`：在 bridge 内构建 `rule_state`，接入 `/api/live/quotes`、SSE 与 `_build_full_snapshot()`。
- `YM-G1A-3`：同步 `LLM_RULES.md` 和每日基线兼容口径，消除 `<40` 仓位上限矛盾。
- `YM-G1A-4`：用隔离测试锁定规则与 API 契约。

### 2. 本包禁止做什么

- 不修改 `store.js`、`widget-registry.js` 或任何 `widgets/*.js`；组件统一消费属于 `Gate 1B`。
- 不重构 `_verify_signals()`，不替换 `[TEXT]` / `[SIGNALS]` 协议；AI schema 与校验属于 `Gate 1C`。
- 不新建健康中心、精简模式、录入操作、止损提醒或复盘功能。
- 不执行真实 `POST /api/sync`、`POST /api/llm`、`POST /api/refresh`、`POST /api/account/correct`。

### 3. 真实数据保留规则，最高优先级

当前 `data/` 内包含真实盘后追加数据、历史测试痕迹和数据库运行文件，主人要求**全部保留**。

- 禁止执行 `git checkout -- data/...`、`git restore data/...`、`git clean`、删除备份文件或覆盖真实数据。
- 禁止编辑、格式化、回写以下所有文件及同目录新增运行文件：
  - `data/pnl.db*`
  - `data/pnl_history.json`
  - `data/llm_insights.json`
  - `data/sentiment_auto.json`
  - `data/cache_dump.json`
  - `data/dashboard_data.json`
  - `data/ymwm_report.json`
  - `data/zt_history.json`
- 所有测试必须使用 `TemporaryDirectory()`、mock CACHE 或注入文件路径，不能靠测试后恢复文件来证明隔离。
- 当前 bridge 正在运行，可能自行追加真实快照。若开发期间 `data/` 发生增量，只报告文件、时间和 diff 归因，不回滚。

开工第一步保存现状，交付时再对比：

```bash
cd /Users/yimu/Documents/YM_Capital/live-dashboard
git status --short -- data > /tmp/ym_gate1a_data_before.status
git diff --binary -- data > /tmp/ym_gate1a_data_before.diff
shasum data/llm_insights.json data/pnl_history.json data/sentiment_auto.json data/pnl.db \
  > /tmp/ym_gate1a_data_before.sha
```

---

## 二、文件地图

| 文件 | 动作 | 责任边界 |
| --- | --- | --- |
| `scripts/rule_engine.py` | 新建 | 规则阈值、输入规范化、纯函数 `evaluate_rule_state()` |
| `scripts/bridge.py` | 修改 | 将 CACHE/SSOT/baseline 适配为输入；将 `rule_state` 加入接口与 LLM snapshot；守住新增 DB 读取路径的连接释放 |
| `scripts/gen_dashboard_data.py` | 最小修改 | 兼容展示的总仓位档位与实时引擎一致；不得继续形成第二套硬规则 |
| `LLM_RULES.md` | 修改 | 人类可读规则文本与 G1A 阈值一致，并声明机器判定来源是 `rule_state` |
| `tests/test_live_rule_engine.py` | 新建 | 纯函数规则矩阵，不读写真实文件 |
| `tests/test_rule_state_api.py` | 新建 | bridge 输出契约回归，使用 mock/temp，不请求真实写接口 |
| `tests/test_rule_engine.py` | 保留/最小调整 | 保留旧 gen 兼容展示测试；只调整已明确冲突的 `<40` 档位预期 |

---

## 三、冻结的 `rule_state` 契约 v1

### 1. 输入契约

`scripts/rule_engine.py` 提供：

```python
def evaluate_rule_state(inputs, now=None):
    """Pure rule evaluation; no I/O, no global cache, JSON-serializable return."""
```

传入 `inputs` 的结构固定如下；缺值使用 `None`，不得把未知值默认为“安全”：

```python
{
    "account": {
        "pnl_pct": -1.25,                 # 当日账户收益百分比，SSOT
        "valuation_complete": True,        # SSOT 估值是否完整
    },
    "risk": {
        "loss_streak": 0,                 # 连亏天数，baseline risk 兼容输入
    },
    "style": {
        "score": 59,                       # 风格总分
        "lianban_pct": 54,                 # 连板资金占比
        "trend_pct": 46,                   # 趋势资金占比
    },
    "sentiment": {
        "emotion_pct": 40.5,              # 情绪值，百分数 0..100
        "previous_emotion_pct": None,      # 前一有效节点/昨日情绪；缺失则 None
        "limit_up_profit_pct": 3.88,       # 昨日涨停收益，百分数
        "broken_board_pct": 75.8,          # 炸板率，百分数；CACHE 中 0.758 必须在 bridge 转为 75.8
        "promotion_pct": 19.8,             # 晋级率，百分数；CACHE 中 0.198 必须在 bridge 转为 19.8
    },
    "freshness": {
        "quotes": "live",                  # live/delayed/stale/dead
        "sentiment": "live",
    },
}
```

### 2. 输出契约

所有字段始终存在，`blocks` / `warnings` 必须为数组；规则命中项必须含证据值，禁止只返回文案。

```python
{
    "version": "g1a-v1",
    "evaluated_at": "2026-05-26T10:00:00",
    "tradable": True,
    "market_regime": "主升",
    "caps": {
        "base_total_pct": 40,
        "total_pct": 40,
        "lianban_pct": 54,
        "trend_pct": 46,
        "first_entry_pct": 10,
    },
    "windows": {
        "w1": {"in_session": True, "buy_allowed": False, "blocks": ["W1_BROKEN_BOARD"]},
        "w2": {"in_session": False, "buy_allowed": False, "blocks": []},
    },
    "blocks": [
        {
            "code": "W1_BROKEN_BOARD",
            "scope": "w1",
            "message": "炸板率超过 W1 上限",
            "evidence": {"broken_board_pct": 75.8, "max_pct": 30},
        }
    ],
    "warnings": [],
}
```

`blocks` 可用 `scope` 只有：`all`、`w1`、`w2`、`position`。
`tradable` 表示是否允许任何新开仓；只命中 `w1` 阻断时仍可为 `True`，由 `windows.w1.buy_allowed` 表示窗口禁止。

### 3. 阈值唯一版本

本表是 G1A 实现和测试的唯一口径。`LLM_RULES.md` 必须同步，不得自行改阈值。

| 规则 code | 输入条件 | scope | 行为 |
| --- | --- | --- | --- |
| `DATA_UNTRUSTED` | `pnl_pct` 缺失、`valuation_complete is not True` 或 quotes freshness 为 `stale/dead` | `all` | `tradable=false`，`total_pct=0`，W1/W2 false |
| `SENTIMENT_STALE` | sentiment freshness 为 `stale/dead` 或核心情绪字段缺失 | `all` | `tradable=false`，`total_pct=0`，W1/W2 false |
| `DAY_STOP` | `pnl_pct <= -3.0` | `all` | `tradable=false`，仓位全部归零 |
| `LOSS_STREAK` | `loss_streak >= 2` | `all` | `tradable=false`，仓位全部归零 |
| `DOUBLE_ICE` | 当前及前一情绪均 `<20`，且前一情绪可得 | `all` | 禁止新开仓，仓位上限归零 |
| `CLIMAX_STOP` | `emotion_pct >= 85` | `all` | 禁止新开仓，仓位上限归零 |
| `CLIMAX_REDUCE` | `80 <= emotion_pct < 85` | warning | `total_pct=floor(base_total_pct/2)`；不伪造全关结论 |
| `FRIDAY_W1` | 周五 | `w1` | W1 false；W2 不因周五自动关闭 |
| `FRIDAY_TREND_CAP` | 周五且 `trend_pct > 15` | `position` | `trend_pct=15`，不关闭 W2 |
| `W1_EMOTION` | `emotion_pct < 60` | `w1` | W1 false |
| `W1_LIMIT_UP_PROFIT` | `limit_up_profit_pct <= 2` | `w1` | W1 false |
| `W1_BROKEN_BOARD` | `broken_board_pct > 30` | `w1` | W1 false |
| `W1_PROMOTION` | `emotion_pct <40` 时 `promotion_pct <15`；否则 `promotion_pct <18` | `w1` | W1 false |
| `W2_ICE` | `emotion_pct <20` | `w2` | W2 false |
| `W2_BROKEN_BOARD` | `broken_board_pct >40` | `w2` | W2 false |

时段：

```python
W1 = 09:30:00 <= now.time() < 10:01:00
W2 = 14:00:00 <= now.time() < 14:51:00
```

总仓位基础档位，解决现存 `<20` 分到底是 `10%` 还是 `20%` 的冲突：

```python
score >= 80 -> 60
score >= 60 -> 50
score >= 40 -> 40
score <  40 -> 20
```

`first_entry_pct` 固定为 `10`；若命中 `scope=all` 的禁止交易规则则为 `0`。

---

## 四、任务步骤

### Task 1：纯函数规则引擎及测试矩阵

**Files:**
- Create: `scripts/rule_engine.py`
- Create: `tests/test_live_rule_engine.py`

- [ ] **Step 1：先写纯函数失败测试**

新建 `tests/test_live_rule_engine.py`，采用 `unittest`，至少实现以下测试名与断言：

```python
import unittest
from datetime import datetime
from scripts.rule_engine import evaluate_rule_state


def valid_inputs(**overrides):
    data = {
        "account": {"pnl_pct": 0.5, "valuation_complete": True},
        "risk": {"loss_streak": 0},
        "style": {"score": 59, "lianban_pct": 54, "trend_pct": 46},
        "sentiment": {
            "emotion_pct": 65, "previous_emotion_pct": 45,
            "limit_up_profit_pct": 3.0, "broken_board_pct": 20,
            "promotion_pct": 22,
        },
        "freshness": {"quotes": "live", "sentiment": "live"},
    }
    for section, values in overrides.items():
        data[section].update(values)
    return data


class LiveRuleEngineTest(unittest.TestCase):
    def test_day_stop_blocks_all_new_buy(self):
        state = evaluate_rule_state(valid_inputs(account={"pnl_pct": -3.0}), datetime(2026, 5, 27, 9, 40))
        self.assertFalse(state["tradable"])
        self.assertEqual(state["caps"]["total_pct"], 0)
        self.assertIn("DAY_STOP", [b["code"] for b in state["blocks"]])

    def test_untrusted_valuation_blocks_all_buy(self):
        state = evaluate_rule_state(valid_inputs(account={"valuation_complete": False}), datetime(2026, 5, 27, 9, 40))
        self.assertIn("DATA_UNTRUSTED", [b["code"] for b in state["blocks"]])

    def test_friday_blocks_only_w1_and_caps_trend(self):
        state = evaluate_rule_state(valid_inputs(), datetime(2026, 5, 29, 14, 10))
        self.assertIn("FRIDAY_W1", [b["code"] for b in state["blocks"]])
        self.assertEqual(state["caps"]["trend_pct"], 15)
        self.assertTrue(state["windows"]["w2"]["buy_allowed"])

    def test_climax_reduce_halves_cap_without_full_stop(self):
        state = evaluate_rule_state(valid_inputs(sentiment={"emotion_pct": 82}), datetime(2026, 5, 27, 14, 10))
        self.assertTrue(state["tradable"])
        self.assertEqual(state["caps"]["base_total_pct"], 40)
        self.assertEqual(state["caps"]["total_pct"], 20)

    def test_broken_board_thresholds_differ_by_window(self):
        state = evaluate_rule_state(valid_inputs(sentiment={"broken_board_pct": 35}), datetime(2026, 5, 27, 9, 40))
        self.assertFalse(state["windows"]["w1"]["buy_allowed"])
        self.assertNotIn("W2_BROKEN_BOARD", [b["code"] for b in state["blocks"]])

    def test_score_below_40_has_twenty_percent_cap(self):
        state = evaluate_rule_state(valid_inputs(style={"score": 10}), datetime(2026, 5, 27, 14, 10))
        self.assertEqual(state["caps"]["base_total_pct"], 20)
```

还必须补充：`LOSS_STREAK`、`DOUBLE_ICE`、`CLIMAX_STOP`、`SENTIMENT_STALE`、W1 情绪/收益/晋级率失败、W2 冰点/炸板率失败、W1/W2 时段边界测试。

- [ ] **Step 2：运行测试并确认先失败**

```bash
python3 -m unittest tests.test_live_rule_engine -v
```

预期：因 `scripts.rule_engine` 不存在或函数未实现而失败。

- [ ] **Step 3：实现最小纯函数**

`scripts/rule_engine.py` 必须具备以下公开接口，辅助函数可自行组织但不得产生 I/O：

```python
from datetime import datetime

RULE_VERSION = "g1a-v1"


def _number(value):
    try:
        return float(value) if value is not None else None
    except (TypeError, ValueError):
        return None


def base_total_cap(score):
    score = _number(score)
    if score is not None and score >= 80:
        return 60
    if score is not None and score >= 60:
        return 50
    if score is not None and score >= 40:
        return 40
    return 20


def _finding(code, scope, message, **evidence):
    return {
        "code": code,
        "scope": scope,
        "message": message,
        "evidence": evidence,
    }


def evaluate_rule_state(inputs, now=None):
    now = now or datetime.now()
    account = inputs.get("account") or {}
    risk = inputs.get("risk") or {}
    style = inputs.get("style") or {}
    sentiment = inputs.get("sentiment") or {}
    freshness = inputs.get("freshness") or {}

    pnl_pct = _number(account.get("pnl_pct"))
    valuation_complete = account.get("valuation_complete") is True
    loss_streak = int(_number(risk.get("loss_streak")) or 0)
    score = _number(style.get("score"))
    lianban_pct = _number(style.get("lianban_pct")) or 0
    trend_pct = _number(style.get("trend_pct")) or 0
    emotion = _number(sentiment.get("emotion_pct"))
    previous_emotion = _number(sentiment.get("previous_emotion_pct"))
    limit_up_profit = _number(sentiment.get("limit_up_profit_pct"))
    broken_board = _number(sentiment.get("broken_board_pct"))
    promotion = _number(sentiment.get("promotion_pct"))

    blocks = []
    warnings = []
    base_cap = base_total_cap(score)
    total_cap = base_cap

    if pnl_pct is None or not valuation_complete or freshness.get("quotes") in {"stale", "dead"}:
        blocks.append(_finding(
            "DATA_UNTRUSTED", "all", "账户估值或行情数据不可信",
            pnl_pct=pnl_pct,
            valuation_complete=valuation_complete,
            quotes_freshness=freshness.get("quotes"),
        ))

    required_sentiment = {
        "emotion_pct": emotion,
        "limit_up_profit_pct": limit_up_profit,
        "broken_board_pct": broken_board,
        "promotion_pct": promotion,
    }
    missing_sentiment = sorted(key for key, value in required_sentiment.items() if value is None)
    if freshness.get("sentiment") in {"stale", "dead"} or missing_sentiment:
        blocks.append(_finding(
            "SENTIMENT_STALE", "all", "情绪数据不完整或过期",
            sentiment_freshness=freshness.get("sentiment"),
            missing=missing_sentiment,
        ))

    if pnl_pct is not None and pnl_pct <= -3.0:
        blocks.append(_finding("DAY_STOP", "all", "单日熔断触发", pnl_pct=pnl_pct, min_pct=-3.0))
    if loss_streak >= 2:
        blocks.append(_finding("LOSS_STREAK", "all", "连亏触发强制空仓", loss_streak=loss_streak, max_days=2))
    if emotion is not None and previous_emotion is not None and emotion < 20 and previous_emotion < 20:
        blocks.append(_finding("DOUBLE_ICE", "all", "连续双冰禁止新开仓",
                               emotion_pct=emotion, previous_emotion_pct=previous_emotion, max_pct=20))
    if emotion is not None and emotion >= 85:
        blocks.append(_finding("CLIMAX_STOP", "all", "极端高潮禁止新开仓", emotion_pct=emotion, min_pct=85))
    elif emotion is not None and emotion >= 80:
        total_cap = base_cap // 2
        warnings.append(_finding("CLIMAX_REDUCE", "position", "高潮保护降半仓",
                                 emotion_pct=emotion, min_pct=80, reduced_total_pct=total_cap))

    if now.weekday() == 4:
        blocks.append(_finding("FRIDAY_W1", "w1", "周五关闭 W1", weekday="周五"))
        if trend_pct > 15:
            trend_pct = 15
            blocks.append(_finding("FRIDAY_TREND_CAP", "position", "周五趋势占比上限 15%",
                                   trend_pct=style.get("trend_pct"), max_pct=15))

    if emotion is not None and emotion < 60:
        blocks.append(_finding("W1_EMOTION", "w1", "W1 情绪不足", emotion_pct=emotion, min_pct=60))
    if limit_up_profit is not None and limit_up_profit <= 2:
        blocks.append(_finding("W1_LIMIT_UP_PROFIT", "w1", "W1 涨停收益不足",
                               limit_up_profit_pct=limit_up_profit, min_pct=2))
    if broken_board is not None and broken_board > 30:
        blocks.append(_finding("W1_BROKEN_BOARD", "w1", "炸板率超过 W1 上限",
                               broken_board_pct=broken_board, max_pct=30))
    if emotion is not None and promotion is not None:
        promotion_min = 15 if emotion < 40 else 18
        if promotion < promotion_min:
            blocks.append(_finding("W1_PROMOTION", "w1", "W1 晋级率不足",
                                   promotion_pct=promotion, min_pct=promotion_min))
    if emotion is not None and emotion < 20:
        blocks.append(_finding("W2_ICE", "w2", "W2 冰点禁止买入", emotion_pct=emotion, min_pct=20))
    if broken_board is not None and broken_board > 40:
        blocks.append(_finding("W2_BROKEN_BOARD", "w2", "炸板率超过 W2 上限",
                               broken_board_pct=broken_board, max_pct=40))

    globally_blocked = any(item["scope"] == "all" for item in blocks)
    if globally_blocked:
        total_cap = 0
        lianban_pct = 0
        trend_pct = 0
    w1_blocks = [item["code"] for item in blocks if item["scope"] in {"all", "w1"}]
    w2_blocks = [item["code"] for item in blocks if item["scope"] in {"all", "w2"}]
    in_w1 = now.replace(hour=9, minute=30, second=0, microsecond=0) <= now < now.replace(hour=10, minute=1, second=0, microsecond=0)
    in_w2 = now.replace(hour=14, minute=0, second=0, microsecond=0) <= now < now.replace(hour=14, minute=51, second=0, microsecond=0)
    regime = "unknown" if emotion is None else (
        "冰点" if emotion < 20 else "低迷" if emotion < 40 else
        "主升" if emotion < 60 else "强势" if emotion < 80 else "高潮"
    )
    return {
        "version": RULE_VERSION,
        "evaluated_at": now.isoformat(timespec="seconds"),
        "tradable": not globally_blocked,
        "market_regime": regime,
        "caps": {
            "base_total_pct": base_cap,
            "total_pct": total_cap,
            "lianban_pct": lianban_pct,
            "trend_pct": trend_pct,
            "first_entry_pct": 0 if globally_blocked else 10,
        },
        "windows": {
            "w1": {"in_session": in_w1, "buy_allowed": in_w1 and not w1_blocks, "blocks": w1_blocks},
            "w2": {"in_session": in_w2, "buy_allowed": in_w2 and not w2_blocks, "blocks": w2_blocks},
        },
        "blocks": blocks,
        "warnings": warnings,
    }
```

实现要求：

- 核心情绪字段 `emotion_pct`、`limit_up_profit_pct`、`broken_board_pct`、`promotion_pct` 任一为 `None` 时，输出 `SENTIMENT_STALE`，不得自动视为 0 后给出可交易结论。
- `blocks` 保留所有命中规则，不因一个全局阻断而停止收集证据。
- `windows.w1/w2.buy_allowed` 必须同时受全局阻断、窗口阻断和是否处于对应时段约束。

- [ ] **Step 4：运行纯函数测试确认通过**

```bash
python3 -m unittest tests.test_live_rule_engine -v
```

预期：全部通过，无 warning。

### Task 2：bridge 适配输入并输出同一份 `rule_state`

**Files:**
- Modify: `scripts/bridge.py`
- Create: `tests/test_rule_state_api.py`

- [ ] **Step 1：先写 bridge 契约失败测试**

新建 `tests/test_rule_state_api.py`。使用 `tempfile`、`patch.object(bridge, "DATA_FILE", tmp_path)` 和 mock `bridge._current_pnl_summary`/`bridge.CACHE`，禁止读取或写入真实 `data/`。

至少覆盖：

```python
class RuleStateBridgeContractTest(unittest.TestCase):
    def test_full_snapshot_contains_rule_state(self):
        snapshot = bridge._build_full_snapshot()
        self.assertIn("rule_state", snapshot)
        self.assertEqual(snapshot["rule_state"]["version"], "g1a-v1")

    def test_live_quotes_response_contains_same_rule_contract(self):
        # 复用 tests/test_correct_api.py 的 mock handler 模式调用 GET /api/live/quotes
        body = json.loads(handler_body)
        self.assertIn("rule_state", body)
        self.assertIn("windows", body["rule_state"])

    def test_cache_decimal_rates_are_converted_to_percent(self):
        # CACHE["iwencai"]={"情绪值": 65, "昨日涨停收益": 3.0,
        #                    "晋级率": 0.198, "炸板率": 0.758,
        #                    "_updated": "2026-05-27T09:40:00+08:00"}
        state = bridge._build_rule_state(now=datetime(2026, 5, 27, 9, 40))
        codes = [b["code"] for b in state["blocks"]]
        self.assertIn("W1_BROKEN_BOARD", codes)  # 75.8% > 30%，不能误当 0.758%

    def test_live_quotes_rule_state_releases_ssot_connection(self):
        # 调用 GET /api/live/quotes 后不得留下 db._local.conn。
        handler.do_GET()
        self.assertIsNone(getattr(db._local, "conn", None))
```

- [ ] **Step 2：运行契约测试确认先失败**

```bash
python3 -m unittest tests.test_rule_state_api -v
```

预期：缺少 `_build_rule_state()` 或响应缺 `rule_state` 而失败。

- [ ] **Step 3：在 bridge 中新增只读适配函数**

新增接口 `_build_rule_inputs(now=None, account_state=None)`：返回第三节定义的输入 dict；当 `account_state` 为空时才调用 `_current_pnl_summary()`。新增接口 `_build_rule_state(now=None, account_state=None)`：仅把 `_build_rule_inputs()` 的结果交给 `scripts.rule_engine.evaluate_rule_state()` 并返回其输出。

适配要求：

- 账户输入使用 `_current_pnl_summary()` 的 `pnl_pct` 和 `valuation_complete`，不使用持仓浮盈冒充当日账户 PnL。
- `CACHE["iwencai"]["晋级率"]`、`炸板率` 为小数来源时转换为百分数；baseline 的 `sentiment` 已是百分数，不重复乘 100。
- 情绪值优先用 `live_index` 上涨/下跌家数计算；不可得时使用 `iwencai["情绪值"]`，再回退 baseline `sentiment["情绪值"]`。
- previous emotion 只能在 baseline/已有节点有明确值时填入，否则 `None`，不得伪造“双冰”。
- freshness 基于现有 `_add_freshness()` 的逻辑或等价只读函数计算；未更新时间不得输出 `live`。
- `_build_rule_inputs(now=None, account_state=None)` 接受可注入的账户状态；`_build_full_snapshot()` 已取得 `pnl_live` 时必须传入复用，避免同一次 LLM snapshot 重复查询 DB。

接入点：

```python
# /api/live/quotes result
result["rule_state"] = _build_rule_state()

# /api/live/stream 每次 result
result["rule_state"] = _build_rule_state()

# _build_full_snapshot() return dict
"rule_state": _build_rule_state(),
```

避免 `_build_full_snapshot()` 与 `_build_rule_state()` 相互调用造成递归。

- [ ] **Step 4：补新增 SSOT 读取的连接生命周期**

接入前 `/api/live/quotes` 和 SSE 仅返回 CACHE，不会查询 SQLite；G1A 加入账户规则后会调用 `_current_pnl_summary()`。因此必须处理连接释放，不得重引入 Gate 0 的句柄泄漏。

```python
# GET /api/live/quotes 分支：取得 rule_state 前初始化，响应发送后释放
_ensure_db()
try:
    result["rule_state"] = _build_rule_state()
finally:
    self._db_close()

# SSE 循环：每一次构造含账户规则的消息后都释放本轮 SQLite 连接，
# 不能将连接留在长期存活的 SSE handler 线程上。
_ensure_db()
try:
    result["rule_state"] = _build_rule_state()
finally:
    self._db_close()
```

实现时可以按现有分支结构调整 `try/finally` 覆盖范围，但以下行为必须成立：

- `/api/live/quotes` 成功、规则计算异常两条路径都关闭 thread-local SQLite 连接。
- SSE 每次循环完成规则计算即关闭连接；客户端持续在线不导致连接一直占用。
- `_build_full_snapshot()` 被 HTTP LLM 路径或 scheduler 调用时，沿用其现有 request/finally 收尾机制，不重复打开第二次账户查询。

- [ ] **Step 5：运行 bridge 契约测试与现有回归**

```bash
python3 -m unittest tests.test_rule_state_api tests.test_account_ssot tests.test_db_lifecycle tests.test_test_isolation -v
```

预期：全部通过，且不改真实 `data/`。

### Task 3：同步文本规则和 baseline 兼容输出

**Files:**
- Modify: `LLM_RULES.md`
- Modify: `scripts/gen_dashboard_data.py`
- Modify: `tests/test_rule_engine.py`

- [ ] **Step 1：调整旧兼容测试的冲突预期**

现有 `tests/test_rule_engine.py::test_total_cap_10` 期待低分为 `10%`，与 `LLM_RULES.md` 及本次 v1 契约冲突。将测试改为低分仍为 `20%`，并改名：

```python
def test_total_cap_below_40(self):
    self.assertEqual(_compute_total_cap({"total": 10}), 20)
```

- [ ] **Step 2：使 gen 的每日展示口径与实时规则一致**

在 `scripts/gen_dashboard_data.py` 中只处理仓位基础档位冲突：

```python
_TOTAL_CAP_BRACKETS = [(80, 60), (60, 50), (40, 40)]

def _compute_total_cap(sd):
    total = sd.get("total", 50) or 50
    for threshold, cap in _TOTAL_CAP_BRACKETS:
        if total >= threshold:
            return cap
    return 20
```

不要在本任务把实时窗口规则重新复制到 gen；实时规则只在 `scripts/rule_engine.py` 执行。
不要执行 `scripts/gen_dashboard_data.py` 生成真实 `data/dashboard_data.json`；这里只运行导入函数的隔离单测。

- [ ] **Step 3：同步 `LLM_RULES.md`**

文本必须增加以下声明并校准冲突：

```markdown
> 机器硬规则唯一来源：`scripts/rule_engine.py` 输出的 `rule_state`。
> 本文件供人类与模型阅读；如文本与 `rule_state` 不一致，以 `rule_state` 为准并视为待修文档缺陷。
```

同时明确：

- `<40` 总仓位上限为 `20%`，不存在 `10%` 档。
- 高潮 `80% <= 情绪 < 85%` 为半仓警告；`>=85%` 禁止新开仓。
- 周五关闭 W1、趋势占比上限 `15%`，不自动关闭 W2。
- 数据不可信、情绪数据 stale/dead 或核心字段缺失时禁止新开仓。

- [ ] **Step 4：运行文本兼容与新引擎测试**

```bash
python3 -m unittest tests.test_rule_engine tests.test_live_rule_engine -v
```

预期：全部通过。

### Task 4：整包验证并停止，等待欧米审查

**Files:**
- Do not modify any new file in this task.

- [ ] **Step 1：检查改动范围**

```bash
git diff --name-only
git status --short
```

预期业务范围仅包含本清单文件及 Gate 0 已有未提交改动；本包新增/修改应只为：

```text
scripts/rule_engine.py
scripts/bridge.py
scripts/gen_dashboard_data.py
LLM_RULES.md
tests/test_live_rule_engine.py
tests/test_rule_state_api.py
tests/test_rule_engine.py
```

不得将任何 `data/` 变化解释为需要恢复或清理的对象。

- [ ] **Step 2：运行完整测试和语法检查**

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -W error::ResourceWarning -m unittest discover -s tests -v
python3 -m compileall -q scripts tests
node --check widgets/position-calc.js
node --check widgets/risk-panel.js
node --check widgets/positions.js
node --check widgets/w1-check.js
node --check widgets/w2-check.js
```

预期：全部通过，无 skip、无 `ResourceWarning`。

- [ ] **Step 3：比较真实数据基线，只报告不处理**

```bash
git status --short -- data > /tmp/ym_gate1a_data_after.status
git diff --binary -- data > /tmp/ym_gate1a_data_after.diff
shasum data/llm_insights.json data/pnl_history.json data/sentiment_auto.json data/pnl.db \
  > /tmp/ym_gate1a_data_after.sha
cmp -s /tmp/ym_gate1a_data_before.status /tmp/ym_gate1a_data_after.status; echo "status cmp=$?"
cmp -s /tmp/ym_gate1a_data_before.diff /tmp/ym_gate1a_data_after.diff; echo "diff cmp=$?"
```

若比较不同：列出新增变化与 mtime；由于 bridge 可能仍在追加真实快照，**只报告，不回滚、不删除、不覆盖**。

- [ ] **Step 4：交付报告格式**

回传欧米以下内容，完成后停下，不进入 Gate 1B/1C：

```markdown
# Gate 1A 交付报告
- 状态：DONE / BLOCKED
- 实际修改文件：
- `rule_state` 输出样例（一个正常场景 + 一个阻断场景）：
- 测试结果：测试数、skip 数、ResourceWarning：
- 语法检查结果：
- data 对比结果：原有文件是否保留；期间是否由运行 bridge 新增快照：
- 未执行事项：未改组件、未改 AI schema、未发送真实 POST、未恢复或删除 data：
```

---

## 五、欧米验收检查点

洋米回传后，欧米只在以下均通过时批准进入 `Gate 1B`：

- [ ] `rule_engine.py` 为纯函数，无 I/O、无 CACHE、无隐式时间/文件依赖（`now` 可注入）。
- [ ] 所有阻断输出具备稳定 `code` 与 `evidence`，阈值与 `LLM_RULES.md` 对齐。
- [ ] `/api/live/quotes`、SSE 和 `_build_full_snapshot()` 使用同一 `rule_state` 生成入口。
- [ ] `/api/live/quotes` 与 SSE 因 `rule_state` 新增的 SSOT 查询在所有路径释放 SQLite 连接，不回退 Gate 0。
- [ ] `DATA_UNTRUSTED`、`DAY_STOP`、`LOSS_STREAK`、`DOUBLE_ICE`、高潮、周五、W1/W2 阈值矩阵均有测试。
- [ ] 小数型 `晋级率` / `炸板率` 在 bridge 适配处转换一次且只转换一次。
- [ ] 完整测试无 warning，测试未写真实数据。
- [ ] 真实 `data/` 及其历史污染/运行追加内容完整保留，未被洋米恢复、删除或覆盖。
