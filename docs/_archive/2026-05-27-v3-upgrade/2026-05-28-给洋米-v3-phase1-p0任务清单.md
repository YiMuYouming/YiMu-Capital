# v3.0 Phase 1 P0：成交写入前端校验与账本防御任务清单

> 派单日期：2026-05-28  
> 派单人：欧米  
> 执行人：洋米  
> 阶段目标：一次性关闭 v3.0 P0，不夹带 P1/P2。  
> 任务基线：`docs/plans/2026-05-28-v3-upgrade-plan.md` 的 Phase 1。

## 任务 ID：YM-V3-PH1-P0

### YM-V3-PH1-P0：关闭成交写入与账户账本 P0 风险

**负责人：** 洋米

**目标：**

1. W15 前端禁止把小数数量、非法价格、空代码/空名称、非法时间改写后提交；尤其禁止 `1.5` 被 `parseInt` 截断成 `1`。
2. 账户 SSOT reducer 遇到历史超卖账本时必须 fail-closed，不能先增加现金再把持仓 clamp 到 0。

**文件范围：**

- 修改：`widgets/positions.js`
- 修改：`scripts/account_ssot.py`
- 修改：`tests/test_frontend_g3b.py`
- 修改：`tests/test_account_ssot.py`

**禁止范围：**

- 禁止修改 `scripts/bridge.py` 的 `/api/sync` 逻辑，本阶段只补前端校验和 reducer 防御。
- 禁止修改 `scripts/db.py`。
- 禁止修改或写入真实 `data/*`。
- 禁止对真实 `http://localhost:8088` 发 POST。
- 禁止顺手做 W23 context、LLM snapshot、health 三态等 P1/P2。

---

## 开工基线

先记录现场，不清理、不恢复：

```bash
cd /Users/yimu/Documents/YM_Capital/live-dashboard
git status --short > /tmp/ym_v3_ph1_p0_before.status
git diff --binary -- data > /tmp/ym_v3_ph1_p0_before.data.diff
git diff --name-only > /tmp/ym_v3_ph1_p0_before.names
```

确认 `data/` 已有变动时，只记录；不要处理。

---

## 执行清单

### Task 1：W15 前端表单校验必须在发请求前阻断

**当前证据：**

- `widgets/positions.js:351` 当前使用：

```js
var price = parseFloat(g('f_price')) || 0, qty = parseInt(g('f_qty')) || 0;
```

这会把 `数量=1.5` 变成 `1`，与后端“数量必须正整数”的语义不一致。

#### 1.1 RED：新增前端校验测试

在 `tests/test_frontend_g3b.py` 的 `W15PendingRetryTests` 后面新增一个测试类：

```python
class W15FormValidationTests(unittest.TestCase):
    """v3 Phase 1: W15 must reject invalid form input before /api/sync."""

    def test_decimal_qty_rejected_before_payload_build(self):
        script = r"""
var r = _validateTradeEntry({
  time:'10:00', action:'W1追涨', stock:'测试', code:'000001',
  price:'10.50', qty:'1.5', window:'W1', reason:''
}, true);
console.log(JSON.stringify({
  ok:r.ok,
  hasQtyError: String(r.error || '').indexOf('数量') >= 0,
  hasEntry: !!r.entry
}));
"""
        result = _run_node(script, files=["widgets/positions.js"])
        self.assertFalse(result.get("ok"), f"小数数量必须阻断: {result}")
        self.assertTrue(result.get("hasQtyError"), f"错误文案应指向数量: {result}")
        self.assertFalse(result.get("hasEntry"), f"非法输入不得产生成交 entry: {result}")

    def test_valid_entry_is_normalized_without_parseint_truncation(self):
        script = r"""
var r = _validateTradeEntry({
  time:'10:00', action:'W1追涨', stock:'测试', code:'000001',
  price:'10.50', qty:'100', window:'W1', reason:'test'
}, true);
console.log(JSON.stringify({
  ok:r.ok,
  price:r.entry && r.entry['价格'],
  qty:r.entry && r.entry['数量'],
  qtyType:typeof (r.entry && r.entry['数量'])
}));
"""
        result = _run_node(script, files=["widgets/positions.js"])
        self.assertTrue(result.get("ok"), f"合法输入应通过: {result}")
        self.assertEqual(result.get("price"), 10.5)
        self.assertEqual(result.get("qty"), 100)
        self.assertEqual(result.get("qtyType"), "number")

    def test_invalid_required_fields_rejected(self):
        script = r"""
var cases = [
  {time:'10:00', action:'W1追涨', stock:'', code:'000001', price:'10', qty:'100', window:'W1', reason:''},
  {time:'10:00', action:'W1追涨', stock:'测试', code:'', price:'10', qty:'100', window:'W1', reason:''},
  {time:'10:00', action:'W1追涨', stock:'测试', code:'000001', price:'Infinity', qty:'100', window:'W1', reason:''},
  {time:'00:01:99', action:'W1追涨', stock:'测试', code:'000001', price:'10', qty:'100', window:'W1', reason:''}
];
var rejected = cases.map(function(c) { return _validateTradeEntry(c, true).ok === false; });
console.log(JSON.stringify({allRejected: rejected.every(Boolean), rejected: rejected}));
"""
        result = _run_node(script, files=["widgets/positions.js"])
        self.assertTrue(result.get("allRejected"), f"空字段/非法价格/非法时间都应阻断: {result}")
```

先运行，必须失败：

```bash
python3 -m unittest tests.test_frontend_g3b.W15FormValidationTests -v
```

预期失败：`_validateTradeEntry is not defined` 或小数数量未被拒绝。

#### 1.2 GREEN：实现最小前端校验

在 `widgets/positions.js` 中新增纯函数，建议放在 `_nowTime()` 前：

```js
function _validateTradeEntry(raw, buy) {
  var time = String(raw.time || '').trim();
  var stock = String(raw.stock || '').trim();
  var code = String(raw.code || '').trim();
  var priceRaw = String(raw.price || '').trim();
  var qtyRaw = String(raw.qty || '').trim();
  var action = String(raw.action || '').trim();
  var windowName = buy ? String(raw.window || '').trim() : '—';
  var reason = String(raw.reason || '').trim();

  if (!/^([01]\d|2[0-3]):[0-5]\d(:[0-5]\d)?$/.test(time)) {
    return { ok: false, error: '时间格式非法，请输入 HH:MM' };
  }
  if (!stock) return { ok: false, error: '标的名称不能为空' };
  if (!code) return { ok: false, error: '代码不能为空' };

  var price = Number(priceRaw);
  if (!Number.isFinite(price) || price <= 0) {
    return { ok: false, error: '价格必须为有限正数' };
  }
  if (!/^[1-9]\d*$/.test(qtyRaw)) {
    return { ok: false, error: '数量必须为正整数' };
  }
  var qty = Number(qtyRaw);
  if (!Number.isSafeInteger(qty) || qty <= 0) {
    return { ok: false, error: '数量必须为正整数' };
  }

  return {
    ok: true,
    entry: {
      '时间': time,
      '动作': action,
      '标的': stock,
      '代码': code,
      '价格': price,
      '数量': qty,
      '窗口': windowName,
      '原因': reason
    }
  };
}
```

然后改 `#f_save` handler：

- 不再用 `parseInt(g('f_qty'))` 构造成交。
- 先调用 `_validateTradeEntry(...)`。
- 校验失败时不生成新的 `event_id`，不设置 `self._pending`，不调用 `_bridgeSync`。
- 校验失败要在弹窗内显示错误；可以新增或复用一个错误区域，文案来自 `result.error`。
- 校验成功后再生成/复用 `self._pendingEvtId`，并把 `event_id` 写入 `result.entry`。

建议错误区 HTML 放在按钮上方：

```html
<div id="f_error" style="display:none;color:var(--danger);font-size:var(--fs-small);margin-top:var(--sp-xs)"></div>
```

校验失败处理：

```js
var errBox = o.querySelector('#f_error');
if (!validation.ok) {
  if (errBox) {
    errBox.textContent = validation.error;
    errBox.style.display = '';
  }
  return;
}
if (errBox) {
  errBox.textContent = '';
  errBox.style.display = 'none';
}
```

运行：

```bash
python3 -m unittest tests.test_frontend_g3b.W15FormValidationTests tests.test_frontend_g3b.W15PendingRetryTests -v
node --check widgets/positions.js
```

通过条件：新增 W15 校验测试通过，原 pending/retry 测试不回退。

---

### Task 2：account reducer 遇到历史超卖必须 fail-closed

**当前证据：**

- `scripts/account_ssot.py:159` 在判断卖出是否超库存前就执行：

```python
cash += trade_cash_effect(trade)
```

- `scripts/account_ssot.py:165-166` 用 `min(qty, old_qty)` 和 `max(0, ...)` clamp，坏账本会被静默吞掉，并且现金已虚增。

#### 2.1 RED：新增账本超卖防御测试

在 `tests/test_account_ssot.py` 增加测试，建议放在现有 R3 逐股盈亏测试附近：

```python
    def test_oversell_trade_fail_closed_without_cash_inflation(self):
        """历史账本超卖：不能先加现金，必须标记 ledger_error / anchor_blocked。"""
        anchor = {
            "date": "2026-05-27", "effective_at": "2026-05-27T09:30:00",
            "cash": 100000, "day_start_asset": 101000,
            "total_deposit": 100000, "source": "previous_close",
            "positions": [{"标的": "TEST", "代码": "000001", "数量": 100, "成本": 10, "状态": "持有"}],
            "_meta": {"day_start_prices": {"000001": 10}},
        }
        trades = [{
            "id": 1, "trade_date": "2026-05-27", "trade_time": "10:00",
            "action": "卖出", "code": "000001", "name": "TEST",
            "price": 20, "qty": 150,
        }]
        quotes = {"000001": {"最新价": 21}, "_updated": "2026-05-27T10:30:00"}

        state = reduce_account_state(anchor, trades, quotes, now="2026-05-27T10:30:00")

        self.assertEqual(state["cash"], 100000, "非法超卖不得增加现金")
        self.assertFalse(state.get("ledger_ok"), f"应标记账本不可信: {state}")
        self.assertTrue(state.get("anchor_blocked"), f"应复用现有阻断语义: {state}")
        self.assertFalse(state.get("valuation_complete"), f"账本错误时估值不得可信: {state}")
        self.assertIn("ledger", state.get("block_reason", ""))
        self.assertEqual(state.get("ledger_errors", [])[0]["code"], "000001")
        self.assertEqual(state.get("ledger_errors", [])[0]["available_qty"], 100)
        self.assertEqual(state.get("ledger_errors", [])[0]["sell_qty"], 150)
```

先运行，必须失败：

```bash
python3 -m unittest tests.test_account_ssot.AccountSSOTTest.test_oversell_trade_fail_closed_without_cash_inflation -v
```

如果类名不是 `AccountSSOTTest`，用实际测试类名；也可以直接跑：

```bash
python3 -m unittest tests.test_account_ssot -v
```

预期失败：当前现金会增加，且没有 `ledger_ok/ledger_errors/anchor_blocked`。

#### 2.2 GREEN：实现 reducer fail-closed

修改 `scripts/account_ssot.py` 的 `reduce_account_state()`：

1. 在交易回放前初始化：

```python
    ledger_errors = []
```

2. 交易循环里不要在 action 分支前统一加现金。改成：

- 卖出：先检查 `position` 和 `old_qty`。
- `qty > old_qty` 时记录 `ledger_errors`，跳过这笔交易的现金和持仓变更。
- 合法卖出才 `cash += trade_cash_effect(trade)`。
- 合法买入才 `cash += trade_cash_effect(trade)`。

建议错误结构：

```python
ledger_errors.append({
    "type": "oversell",
    "trade_id": trade.get("id"),
    "code": code,
    "name": trade.get("name", ""),
    "trade_time": trade.get("trade_time", ""),
    "sell_qty": qty,
    "available_qty": old_qty,
})
```

3. 返回对象中增加：

```python
        "ledger_ok": not ledger_errors,
        "ledger_errors": ledger_errors,
```

4. 如果有 `ledger_errors`，必须同时：

```python
        "anchor_blocked": True,
        "block_reason": "ledger_error: oversell detected",
        "valuation_complete": False,
```

注意：

- 不要抛异常中断 `/api/account/state`；要返回 fail-closed 状态，让 `/api/health` 走已有 `anchor_blocked` 分支。
- 不要把非法卖出部分按 `old_qty` 部分成交；坏账本要整笔拒算。
- 不要改 `insert_trade_with_context` 的原子卖出门禁，那是已有后端写入防线。

运行：

```bash
python3 -m unittest tests.test_account_ssot -v
python3 -m compileall -q scripts tests
```

---

## 欧米验收

洋米交付前必须跑完：

```bash
cd /Users/yimu/Documents/YM_Capital/live-dashboard
python3 -m unittest tests.test_frontend_g3b tests.test_account_ssot tests.test_sync_guard tests.test_frontend_w22 tests.test_health_api -v
python3 -m compileall -q scripts tests
node --check widgets/positions.js
git diff --name-only -- data/
git status --short
```

**通过条件：**

- `数量=1.5` 在前端校验阶段被拒绝，不调用 `/api/sync`，不生成 `event_id`。
- 合法 W15 表单仍能构造与后端一致的 entry：价格 number，数量 number。
- reducer 遇到历史超卖时不增加现金，输出 `ledger_ok=false`、`ledger_errors`、`anchor_blocked=true`、`valuation_complete=false`。
- 既有 `tests.test_sync_guard` 仍通过，说明后端写入门禁未回退。
- `git diff --name-only -- data/` 没有新增本任务造成的数据改动；如果开工前已有数据 diff，要说明与 `/tmp/ym_v3_ph1_p0_before.data.diff` 一致。

---

## 交付格式

完成后回传：

```text
状态：DONE / DONE_WITH_CONCERNS / BLOCKED
任务：YM-V3-PH1-P0

实际修改文件：
- ...

每项需求对应实现：
- W15 小数数量前端阻断 → 已实现，验证：
- reducer 超卖 fail-closed → 已实现，验证：

RED 测试命令及失败摘要：
- ...

GREEN/完整验证命令及结果：
- ...

git status --short 输出：
...

git diff --name-only -- data/ 输出：
...

是否触碰真实 8088 或真实 data：
否 / 是（如是，说明原因）

尚存风险或需欧米决策的问题：
- ...
```

交付后停下，等欧米复核。不要继续 Phase 2。
