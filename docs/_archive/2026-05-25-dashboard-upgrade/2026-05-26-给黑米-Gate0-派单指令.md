# 给黑米的第一包派单指令：Gate 0

> 用法：将下方代码块完整复制到 Cursor 对话发送给黑米。
> 当前只执行 Gate 0；黑米交付后先交欧米验收，不继续做 Gate 1。

```text
黑米，执行 live-dashboard 升级第一包：HM-G0A / HM-G0B / HM-G0C（Gate 0 现网止血与验证基线）。

项目目录：
/Users/yimu/Documents/YM_Capital/live-dashboard

开工前必须先读：
1. /Users/yimu/Documents/YM_Capital/live-dashboard/AGENTS.md
2. /Users/yimu/Documents/YM_Capital/live-dashboard/docs/audit/2026-05-26-升级改造执行审查与任务清单.md

当前事实，不要重复猜测：
- main 当前已有账户资产 SSOT。
- 运行中的 `/api/account/state` 已实测返回 500，错误包含 `Too many open files`。
- `/api/health` 目前为 404，是 Gate 2 才做的功能，本包禁止顺手实现。
- 现有账户 SSOT 单测会污染真实 `data/pnl_history.json`，本包必须优先解决测试隔离。
- 当前仓库可能已有未跟踪 `data/**` 运行产物；不得删除、覆盖、提交或清理它们。

本包目标：
先让项目具备可重复、不污染实盘的验证底座；修复账户 API 的句柄耗尽和纠错接口方法错误；修复三个明确的前端显示/时间判断错误。不要扩展功能。

严格范围：

HM-G0A：运行环境、路径解析、测试隔离
- 建立 Python 依赖声明，可选 `requirements.txt` 或 `pyproject.toml`，至少覆盖项目测试/运行所需的 `pytest`、`apscheduler`、`filelock` 及 `ym_stock_data` 的安装说明。
- 更新 `README.md`，给出 venv 安装、启动、测试命令；不要再暗示 Python 侧无依赖。
- 从活动脚本移除硬编码 `/Users/YouMing/Documents/YM_Capital/YM-data-pipeline`：
  - `scripts/collectors/quotes.py`
  - `scripts/collectors/iwencai_poll.py`
  - `scripts/collectors/market_data.py`
  - `scripts/snapshot_auction.py`
  - `scripts/style_detect.py`
  - `scripts/poll_iwencai.py`
  使用明确的环境变量或项目已有配置模式；缺配置时给出可操作错误，不静默换假路径。
- 新增 `scripts/check_runtime.py`：检查关键 Python 包、必要数据文件可读性、SQLite `PRAGMA integrity_check`、端口占用、LLM 配置是否存在。只报告 token 是否存在，绝不能输出 token 值。
- 修改 `scripts/account_ssot.py` 与相关测试：`generate_closing_anchor()` 的 `pnl_history` 写入目标必须可注入；测试必须使用临时文件，运行测试后真实 `data/` 不发生 tracked 变化。

HM-G0B：SQLite 生命周期与纠错 API
- 检查 `scripts/db.py` 当前 thread-local SQLite 连接实现，增加显式释放当前线程连接的方法；释放后必须清空 thread-local 引用。
- 修改 `scripts/bridge.py`：普通 HTTP 请求结束和会接触 DB 的 scheduler job 结束后均释放其当前线程连接。
- 保留现有 WAL 策略，不要在没有专门证据时更改 journal mode。
- 把 `/api/account/correct` 的写操作从 GET 移到 POST：
  - GET 必须返回 `405 Method Not Allowed`，且不写数据库；
  - POST 缺 body 或非法参数返回 `400`；
  - 有效 POST 才新增纠错记录。
- 添加隔离测试或测试 harness，证明对临时 DB/隔离 handler 重复读取账户状态不会导致 `pnl.db`/WAL 文件句柄线性增长。

HM-G0C：明确前端 bug
- 只修改以下文件：
  - `widgets/position-calc.js`
  - `widgets/risk-panel.js`
  - `widgets/positions.js`
  - `widgets/w1-check.js`
  - `widgets/w2-check.js`
- `cash=0`、`mv=0`、`total_asset=0` 是合法 SSOT 值，不能因为 `parseFloat(value) || fallback` 回退到旧基线。
- W09 中 `赚钱效应=不好` 或其他负向状态不得因为包含“好”而通过允许条件。
- W08 时间窗口只覆盖 `09:30:00` 到 `10:00:59`；09:20 不得锁定 W1 开盘数据。

禁止范围：
- 不实现 `rule_engine.py`、`rule_state`、AI schema、`/api/health`、DataStore 重构、顶栏、精简模式、复盘功能。
- 不修改任何 `data/**` 文件。
- 不执行真实 `localhost:8088` 的 POST、纠错、日结或压力请求。
- 不 `git reset --hard`、不 `git clean`、不回退他人已有改动。
- 不把运行产物、缓存、数据库或快照纳入提交。

开发方式：
1. 先运行 `git status --short` 记录开工基线；遇到已有改动要保留并兼容。
2. 严格 TDD：每个行为先新增或调整测试，并运行确认是因缺少该行为而失败；再写最小实现；最后运行 GREEN。
3. 在完成测试隔离以前，不得运行会触发日结写真实文件的旧账户单测。
4. 若某个要求需要改到禁止范围，停止并报告 BLOCKED，不要自行扩大范围。

最低验收输出：
- 针对测试隔离、纠错方法、连接释放分别提供 RED -> GREEN 命令及结果摘要。
- 完整运行 Python 测试并确认运行前后 `git diff -- data` 无新增差异。
- 运行：
  python3 -m compileall -q scripts
  node --check widgets/position-calc.js
  node --check widgets/risk-panel.js
  node --check widgets/positions.js
  node --check widgets/w1-check.js
  node --check widgets/w2-check.js
  rg -n '/Users/YouMing' scripts --glob '!_archive/**'
- 接口压力验证必须使用临时 DB/临时数据目录及隔离实例或测试 handler，不得压真实 8088。

完成后停下，不要继续 Gate 1。按以下格式汇报给我，让欧米验收：

状态：DONE / DONE_WITH_CONCERNS / BLOCKED
任务：HM-G0A / HM-G0B / HM-G0C
实际修改文件：
每项需求对应实现：
RED 测试命令及失败摘要：
GREEN/完整验证命令及结果：
`git status --short` 输出：
`git diff -- data` 是否为空：
是否触碰真实 8088 或真实数据：
尚存风险或需欧米决策的问题：
```
