# Handoff: SYNC-R4 + SSOT-R2 (2026-05-27)

## Current state
- bridge running, PnL restored (9 snapshots), API: cash=100187, total_asset≈210000
- 322 tests OK, compileall OK, JS syntax OK
- Real data manually corrected (deleted fake trades id=24/25, fixed anchor, restored snapshots)

## Completed

### DB (scripts/db.py)
- trade_records: event_id column + UNIQUE index (WHERE NOT NULL AND !='')
- Old idx_tr_uniq now conditional (WHERE event_id IS NULL)
- insert_trade_with_context returns (inserted, trade_id, status): inserted/idempotent/duplicate
- IntegrityError only returns idempotent if same event_id found, otherwise re-raises

### Sync (scripts/bridge.py)
- /api/sync accepts single entry only, rejects 今日操作 batches (409)
- Entry with positions rejected (400), positions-only rejected (409)
- Sell qty > available rejected (409), future time rejected (400)
- Response includes trade_id + status

### Frontend (widgets/positions.js)
- Payload: { entry: entry } only, no positions
- event_id: w15-{ts}-{rand}, stable across retries
- Failure rolls back optimistic position state

### Per-stock PnL (scripts/account_ssot.py)
- reduce_account_state tracks today_pnl_map[code] (realized from sells) + today_basis_map[code] (denominator)
- New buy only: (price-cost)*qty, denominator=buy_amount
- Overnight: (price-day_start_price)*qty+realized, denominator=day_start_value+buy_amount
- No day_start_price: today_pnl=None
- closed_positions only when qty -> 0 (fully sold)
- generate_closing_anchor writes _meta.day_start_prices for next day

### Anchor trust (scripts/account_ssot.py)
- ensure_today_anchor: recovery+positions blocked, no-prev_close+positions blocked
- load_current_account_state returns anchor_trusted bool
- Untrusted -> valuation_complete=false -> DATA_UNTRUSTED

### Test files
- tests/test_sync_guard.py
- tests/test_anchor_integrity.py
- tests/test_trade_review.py
- tests/test_account_ssot.py (紫光 today_pnl=900 case, closed_positions)

## Next (do NOT implement yet, wait for review)
1. W15 display: SSOT-only, column order: 标的|市值|现价|成本|今日盈亏|累计盈亏|止损
2. Price format: 87.00, amounts with commas
3. W15/W17 use trade_records only, show "今日无操作" when empty
4. Closed position tracking: SSOT closed_positions, 7-day window
5. XSS escaping on all user text fields
