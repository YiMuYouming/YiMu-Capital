# 给黑米的 Gate 0 三次返工指令

> 用法：将下方代码块完整复制给 Cursor 中的黑米。
> 结论：Gate 0 第三次验收未通过；仅修剩余阻断，禁止进入 Gate 1。

```text
黑米，执行 `HM-G0-R3`。Gate 0 仍未通过，本轮只修下面四类剩余阻断，已通过部分不要回退。

项目：
/Users/yimu/Documents/YM_Capital/live-dashboard

已经由欧米独立验证通过的内容，请保留：
- 六个采集器默认 `_load_pipeline_path()` 当前都返回存在的 `/Users/yimu/Documents/YM_Capital/YM-data-pipeline`。
- 六个采集器显式设置不存在的 `YM_DATA_PIPELINE_PATH` 时，其自身 `_load_pipeline_path()` 会抛 `RuntimeError`。
- 临时空库的 `GET /api/trades` 现在返回 200，自动创建 7 张表，结束后 thread-local 连接为空。
- `/api/account/correct` 使用 `correction_*` 字段，临时 DB 验证 reversal 为 `卖出 / 120.0 / 100`。
- W03/W14/W15 零值、W08 时间窗口、W09 枚举修复保持不变。

仍阻断 Gate 0 的问题与复现证据：

1. 空库首次交易写入 `/api/sync` 仍失败。
欧米使用临时 `DB_PATH` 与临时 `DATA_FILE` 直接调用 `POST /api/sync`，payload 含首笔 `今日操作`，实际结果：
`fresh_sync_status=400 body={"error": "no such table: account_baselines"} tables=0`

根因：
`_ensure_db()` 当前只在部分 GET 路由调用；`do_POST()` 的 `/api/sync` 在调用 `load_current_account_state()` 和 `insert_trade()` 前没有初始化 schema。

必须修：
- 所有可能首先触碰 DB 的写入口在使用 DB 前调用统一初始化，包括至少 `/api/sync`、`/api/account/correct`、会持久化 LLM 的 POST 路径。
- 初始化不能重新引入长驻请求线程连接；请求结束仍须释放。
- 添加回归测试：全新临时 DB + 临时 `DATA_FILE`，首笔 `/api/sync` 返回成功、表已建、成交存在、JSON 仅写临时文件、thread-local 连接已释放。

2. `scripts/check_runtime.py` 仍给出假阳性。
欧米验证：
- 在 `YM_DATA_PIPELINE_PATH=/tmp/does-not-exist-yimucodex` 下调用 `check_runtime._check_collector_paths()`，仍返回 `collector_check_bad_env_exit=0`。
- 它输出 `quotes.py: 默认路径 OK (Documents)` 等结果，而不是 `YM-data-pipeline`，说明它自行猜路径并因环境中已可导入包而误判，根本没有调用被检脚本的 `_load_pipeline_path()`。

必须修：
- `_check_collector_paths()` 用 `importlib` 实际加载每个脚本并调用该脚本的 `_load_pipeline_path()`；验证返回路径存在，并从该解析路径验证 `ym_stock_data` 可导入。
- 保证显式无效 `YM_DATA_PIPELINE_PATH` 会让 `check_runtime` 返回非零并报告对应脚本失败。
- 添加测试：正常默认路径成功；显式无效环境变量时 runtime 检查失败，不能只测试各脚本函数。

3. `tests/test_test_isolation.py` 仍违反“不得接触真实 DB”。
证据：
- 第 125-139 行打开 `self.orig_path` 指向的真实 `data/pnl.db`。
- 第 160-174 行再次打开真实 `data/pnl.db`。
- 第 186-223 行的 `GitDataIsolationTest` 在同一个测试方法中连续计算 before/after hash，中间未运行任何待测行为，因此永远无法证明测试套件没有污染数据。

当前数据状态也与“data/** 未改动”回报不符：
- `data/pnl.db` 当前有 binary diff。
- `data/sentiment_auto.json` 当前新增运行节点。
- `data/pnl_history.json` 当前也有 tracked diff，内容为 `last_total_asset=111400.0`、`last_mv=17000`、`last_updated=2026-05-26T15:05:00`；与日结回放 fixture 数值吻合，但欧米本轮仅确认当前 `tests.test_account_ssot` 再运行不会进一步改写，未擅自恢复现有差异。

必须修：
- 删除测试中打开真实 `data/pnl.db` 的代码；隔离测试只能验证注入后的临时 DB/临时文件路径和结果。
- 删除无效 `GitDataIsolationTest`，或改为在单个 subprocess 内对明确的待测命令前后采样，并且此时不得有活动 bridge 并发写入；当前阶段优先删除，真实 data 状态由欧米外部验收。
- 不修改、不恢复、不提交当前 `data/**` 差异；报告中明确列为已有运行/待人工确认的工作树变化。

4. LLM / job 生命周期测试名不副实。
证据：
`tests/test_db_lifecycle.py:227-278` 的 `LLMSchedulerConnectionReleaseTest` 没有调用 `trigger_llm_auto()`，只是直接调用 `db.insert_llm()` 后再手工 `db.close_conn()`，不能证明 [scripts/bridge.py] 的 finally 真的覆盖 early-return / insert-error。
同样尚未看到直接执行 closing anchor / morning health 回调并断言释放的回归测试。

必须修：
- 将需要测试的调度回调提取为可导入的模块级函数，或提供等效可直接调用的最小封装；不要仅凭嵌在 `__main__` 中无法测试的函数声称覆盖。
- 测试必须实际调用生产回调：
  - `log_pnl_snapshot()` 成功与 incomplete 分支；
  - closing anchor 回调异常/成功后连接释放；
  - morning health 回调存在/缺失锚点分支后连接释放；
  - LLM 自动流程在 API 失败 early-return 与 `insert_llm` 抛错时连接释放。
- 全部使用临时 DB、临时数据路径和 mock 外部 API，不访问真实 8088、不写真实 `data/**`。

验证与交付要求：
- 先新增失败测试，提供上述四项 RED 摘要；再修代码并提供 GREEN 摘要。
- 跑 `python3 -m unittest discover -s tests -v`，报告测试数、skip 数和 ResourceWarning；禁止把真实 HTTP 500 静默 skip 当成功。
- 跑 `python3 -m compileall -q scripts` 与：
  node --check widgets/position-calc.js
  node --check widgets/risk-panel.js
  node --check widgets/positions.js
  node --check widgets/w1-check.js
  node --check widgets/w2-check.js
- 单独给出以下验收结果：
  1. 临时空库首笔 `/api/sync` 成功、表/成交已写入临时位置、连接释放；
  2. `YM_DATA_PIPELINE_PATH=/tmp/does-not-exist` 时 `check_runtime` 非零；
  3. `rg -n \"sqlite3.connect|orig_path|data/pnl.db\" tests/test_test_isolation.py` 不再显示真实 DB 读取验证逻辑；
  4. 四类后台生产回调均由真实测试调用，而不是手工 close 自证。
- 不运行真实 8088 的写请求或压力测试。
- 不修改、恢复、提交 `data/**`；这些现有 dirty 数据由欧米与主人另行处置。
```
