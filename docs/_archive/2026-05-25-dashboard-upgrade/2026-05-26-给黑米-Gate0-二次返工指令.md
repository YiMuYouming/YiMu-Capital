# 给黑米的 Gate 0 二次返工指令

> 用法：将下方代码块完整复制给 Cursor 中的黑米。
> 结论：Gate 0 第二次验收仍未通过；保留已验证修复，只处理剩余阻断。

```text
黑米，Gate 0 第二次验收仍未通过。请执行 `HM-G0-R2`，完成后停下等待欧米复验，不进入 Gate 1。

项目：
/Users/yimu/Documents/YM_Capital/live-dashboard

已确认正确、不要回退：
- 六个采集器未再出现 `/Users/YouMing` 写死路径，默认 sibling 路径现可解析到 `/Users/yimu/Documents/YM_Capital/YM-data-pipeline`。
- W03/W14/W15 的 SSOT 零值修复、W08 时间窗口、W09 精确枚举保留。
- `/api/account/correct` 目前读取 `correction_action` / `correction_price` / `correction_qty`；欧米用临时 DB 复现得到 reversal `卖出 / 120.0 / 100`，实现本体已正确。
- `data/pnl.db` 与 `data/sentiment_auto.json` 当前已有运行中 bridge 写入痕迹；不得清理、回退或提交这些文件，也不得声称是本次返工修复内容。

仍未通过的阻断项：

1. 新增 `_ensure_db()` 未被调用，首次空库启动失败。
证据：
`rg -n "_ensure_db\\(|init_db\\(" scripts/bridge.py` 仅显示 `_ensure_db` 定义，无任何调用。
欧米在临时空数据库直接调用 `GET /api/trades`，得到：
`500 {"error": "no such table: trade_records"}`

必须修：
- 明确数据库初始化生命周期：首次需要 DB 的 API/job 前保证 schema 已初始化，同时请求/job 结束仍关闭线程连接。
- 增加临时空库回归测试：无需预先手动 `db.init_db()`，首个相关 API 请求即可使用已创建的 schema，并在结束后没有遗留 thread-local 连接。
- 不以重新恢复模块 import 时长期持有连接来绕过释放问题。

2. 自动 LLM 调度仍可能泄漏 DB 连接。
证据：
- `trigger_llm_auto()` 在 `_build_full_snapshot()` 阶段就可能访问账户/摘要 DB。
- 当前只在 `insert_llm()` 成功之后调用 `close_conn()`；若 LLM 请求失败、提前 return、或 DB insert 抛错，已有连接不一定关闭。

必须修：
- 将 `trigger_llm_auto()` 整条可能触 DB 的执行路径包在 `finally: close_conn()` 中。
- 添加测试覆盖“snapshot 后 LLM 返回失败/提前 return”与“insert_llm 抛错”两条路径，断言连接已释放。
- 检查 `log_pnl_snapshot()` 当前 `row is None` 分支和 `finally` 重复 close，可简化为统一 finally，避免测试噪音，但不得改变业务输出。

3. 无效环境变量仍静默回退，runtime 检查仍漏检运行路径。
证据：
设置 `YM_DATA_PIPELINE_PATH=/tmp/does-not-exist-yimucodex` 后，六个 `_load_pipeline_path()` 仍悄悄返回默认路径。
`scripts/check_runtime.py:_check_collector_paths()` 仍只是 `compile()` + grep，不加载脚本、不调用路径解析，因此无法证明运行可用。

必须修：
- 若用户显式提供 `YM_DATA_PIPELINE_PATH` 但路径不存在，立即给出明确错误，不要静默忽略配置。
- `check_runtime.py` 实际加载六个脚本并验证路径解析结果存在且能用于导入所需模块；无效环境变量必须被报告为失败。
- 添加自动测试覆盖默认有效路径与显式无效环境变量错误。

4. `tests/test_test_isolation.py` 仍不是有效守门，并且会主动接触真实数据库。
证据：
- `DataDirIsolationTest` 的 hash 基于 mtime/size，不是内容。
- 它在 `tearDownClass()` 里发生变化后再读取同一文件的 before/after stat，无法定位原变化。
- `test_no_real_db_writes()` 直接调用 `scripts.db.get_conn()`，会连接真实 `data/pnl.db`；活动 bridge 运行时这一测试无法证明变动来源。

必须修：
- 测试不得打开、查询或写入真实 `data/pnl.db`。
- 删除这套伪守卫，或重写为真正隔离的测试：只在临时 data/临时 DB 上执行写路径，并断言 `generate_closing_anchor` / API / job 使用注入路径。
- “真实 data 无变化”作为欧米外部验收检查执行；在 bridge 活动写入期间不得通过一个单测声称已证明。

5. DB 生命周期测试覆盖不足。
证据：
`tests/test_db_lifecycle.py` 仍只测试手工调用 `db.close_conn()`，未直接调用 BridgeHandler 或 scheduler/job 路径；它不能证明泄漏根因被修掉。

必须修：
- 补 handler 结束后释放连接测试。
- 补上述 DB job 的释放测试，至少覆盖 `log_pnl_snapshot`、closing anchor、morning health、LLM early-return。
- 全部使用临时 DB/临时数据文件；不得向真实 8088 发压力或写请求。

范围收敛：
- 不开展 `rule_state`、AI schema 重构、`/api/health`、DataStore/界面扩展。
- 对已经改写的 `tests/test_api.py`、`tests/test_gen_baseline.py`、`tests/test_pnl_calc.py`、`tests/test_rule_engine.py`：不要再扩大改动；若保留 unittest 迁移，确保原断言没有丢失，并在交付报告注明这是前次范围扩张遗留，待提交前由欧米决定是否纳入。
- 不修改、恢复或提交 `data/**` 当前变化。

回报必须包含：
1. 实际修改文件列表。
2. 临时空库首个 DB API 成功且连接释放的测试输出。
3. LLM early-return / insert error 仍释放连接的测试输出。
4. 显式无效 `YM_DATA_PIPELINE_PATH` 被拒绝的测试与 `check_runtime` 输出。
5. 说明隔离测试不再打开真实 `data/pnl.db` 的代码位置。
6. `python3 -m unittest discover -s tests -v` 输出摘要和是否仍有 `ResourceWarning`。
7. `python3 -m compileall -q scripts` 与五个前端文件 `node --check` 结果。
8. 明确声明未触碰/恢复 `data/**`、未对真实 8088 发写请求或压力请求。
```
